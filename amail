#!/usr/bin/env python3
"""amail entry point; resolves its installed source even when called by symlink."""
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parent))
from cli import main
raise SystemExit(main())
